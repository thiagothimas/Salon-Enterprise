
<div class="titulo_cad">
    <center><h1>Cadastrar Fornecedor</h1></center>
</div>

<form action="/Fornecedor/Adiciona" method="post">
    @Html.AntiForgeryToken()
    @*Html.ValidationMessage("produto.Invalido")*@


    <div class="form_geral">

        <div class="form_geral">

            <div class="form_geral_2">

                <label for="nome">Nome:</label>
                <input id="nome" name="fornecedor.Nome" class="form-control" />
                @*<input id="nome" name="fornecedor.Nome" value="@ViewBag.Fornecedor.Nome"/>*@
                @Html.ValidationMessage("fornecedor.Nome")

            </div>

            <div class="form_geral_2">

                <label for="cnpj">CNPJ:</label>
                <input id="cnpj" name="fornecedor.Cnpj" class="form-control" />
                @*<input id="cnpj" name="fornecedor.Cnpj" value="@ViewBag.Fornecedor.Cnpj"/>*@
                @Html.ValidationMessage("fornecedor.Cnpj")

            </div>

                @*Adicionar combo_box para determinar uma categoria do produto*@
                @*<label for="categoria">Categoria:</label>
            <select id="categoria" name="produto.categoriaId">
                @foreach (var categoria in ViewBag.Categorias)
                {
                    <option value="@categoria.Id" selected="@categoria.Id.Equals(ViewBag.Produto.CategoriaId)">
                    @categoria.Nome
                    </option>
                }
            </select>*@

            </div>

        </div>

    <div class="btn-cad-voltar">

        <input type="submit" value="Cadastrar" />
        <a href="/Fornecedor/Index" class="btn-voltar" role="button">Voltar</a>

    </div>

</form>
