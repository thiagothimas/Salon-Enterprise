@model IList<SalonEnterpriseAplication.Models.Produto>

<table class="table table-hover">
    <thead>
        <tr>
            <th>Nome do Produto</th>
            <th>Preco</th>
            <th>Tipo</th>
            <th>Quantidade</th>
            <th>Descricao</th>
            <th>Remover Produto</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var produto in Model)
        {
        <tr>

            <td>
                @*Tem valores no banco que o nome ou o Id esta vazio por isso esta dando erro*@
                @Html.ActionLink(produto.Nome, "Visualiza", new { id = produto.Id })
                @*Html.RouteLink(produto.Nome, "VisualizaProduto", new { id = produto.Id })*@
            </td>
            <td>@produto.Valor</td>
            <td>@produto.Tipo</td>
            <td>@produto.Quantidade</td>
            <td>@produto.Descricao</td>
            
            <td>
                @*<a href="/Produto/DecrementaQtd/@produto.Id">Decrementar</a>*@
                @*<a href="#" onclick="decrementa(@produto.Id)">Decrementar</a>*@

                <a class="far fa-trash-alt" href="/Produto/RemoverProduto/@produto.Id"></a>
            </td>
        </tr>
        }
    </tbody>
</table>
<a href="/Home/Index" class="btn btn-primary">Menu Principal</a>
@*<script type="text/javascript" src="~/Scripts/jquery-1.10.2.js"></script>
    <script type="text/javascript">
        function decrementa(produtoId) {
            var url = "@Url.Action("DecrementaQtd", "Produto")";
            $.post(url, { id: produtoId }, atualiza);
        }

        function atualiza(resposta) {
            $("#quantidade" + resposta.Id).html(resposta.Quantidade);
        }
    </script>*@

